# Server config
PORT=8080
HOST=0.0.0.0

# CORS options:
# - GET_ONLY -> allow only GET/HEAD/OPTIONS from anywhere
# - * -> allow any origin and method
# - comma separated list of origins -> e.g., https://example.com,https://foo.bar
CORS_ORIGIN=GET_ONLY

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60

# Body size limit
BODY_LIMIT=10kb

# Optional bearer token for all routes. If set, all requests must send:
# Authorization: Bearer <AUTH_TOKEN>
AUTH_TOKEN=

# Trust proxy hops (needed for accurate client IP behind tunnels). Use 1 for one hop, or 'true'.
TRUST_PROXY=1

# Public exposure using Localtunnel
# Set to 1 to enable a public URL so others on the internet can reach your Mac.
ENABLE_TUNNEL=0
# Optional preferred subdomain. Leave empty for random.
TUNNEL_SUBDOMAIN=

# Geocoding provider
# Base URL for Nominatim-compatible geocoding API used by /geocode/bbox
# Default: https://nominatim.openstreetmap.org
GEOCODER_BASE_URL=
